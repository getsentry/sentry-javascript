<div class="section">
  <h4>{{@title}}</h4>
  <div class="table">
    {{#each @rowItems as |rowItem|}}
    <div class="table-row">
      <span>{{rowItem}}</span>
    </div>
    {{/each}}
  </div>
</div>
