{
  "name": "@sentry/nextjs",
  "version": "6.2.1",
  "description": "Official Sentry SDK for Next.js",
  "repository": "git://github.com/getsentry/sentry-javascript.git",
  "homepage": "https://github.com/getsentry/sentry-javascript/tree/master/packages/nextjs",
  "author": "Sentry",
  "license": "MIT",
  "engines": {
    "node": ">=6"
  },
  "module": "./esm/index.server.js",
  "browser": "./esm/index.client.js",
  "types": "./esm/types.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@sentry/core": "6.2.5",
    "@sentry/integrations": "6.2.5",
    "@sentry/next-plugin-sentry": "6.2.1",
    "@sentry/node": "6.2.5",
    "@sentry/react": "6.2.5",
    "@sentry/webpack-plugin": "^1.14.1",
    "@sentry/wizard": "^1.2.3"
  },
  "devDependencies": {
    "@sentry/types": "6.2.5",
    "@sentry/utils": "6.2.5",
    "@types/prettier": "^2.2.3",
    "chokidar-cli": "^2.1.0",
    "eslint": "7.20.0",
    "rimraf": "3.0.2"
  },
  "scripts": {
    "build": "run-s build:esm",
    "build:scripts": "tsc -p tsconfig.scripts.json",
    "build:types": "run-s build:scripts generate:types",
    "build:es5": "run-s build:types tsc:es5",
    "build:esm": "run-s build:types tsc:esm",
    "build:watch": "run-p build:watch:esm",
    "build:watch:scripts": "tsc -p tsconfig.scripts.json -w --preserveWatchOutput",
    "build:watch:types": "run-p build:watch:scripts generate:types:watch",
    "build:watch:esm": "run-p build:watch:types \"tsc:esm -w --preserveWatchOutput\"",
    "generate:types": "node scripts/build/generate-types.js",
    "generate:types:watch": "chokidar scripts/build/generate-types.js -c \"node scripts/build/generate-types.js\"",
    "tsc:es5": "tsc -p tsconfig.build.json",
    "tsc:esm": "tsc -p tsconfig.esm.json",
    "clean": "rimraf dist esm coverage *.js *.js.map *.d.ts",
    "link:yarn": "yarn link",
    "lint": "run-s lint:prettier lint:eslint",
    "lint:prettier": "prettier --check \"{src,test,scripts}/**/*.ts\"",
    "lint:eslint": "eslint . --cache --cache-location '../../eslintcache/' --format stylish",
    "fix": "run-s fix:eslint fix:prettier",
    "fix:eslint": "eslint . --format stylish --fix",
    "fix:prettier": "prettier --write \"{src,test,scripts}/**/*.ts\"",
    "test": "jest",
    "test:watch": "jest --watch",
    "pack": "npm pack"
  },
  "volta": {
    "extends": "../../package.json"
  },
  "jest": {
    "collectCoverage": true,
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "moduleFileExtensions": [
      "js",
      "ts"
    ],
    "testEnvironment": "node",
    "testMatch": [
      "**/*.test.ts"
    ],
    "globals": {
      "ts-jest": {
        "tsConfig": "./tsconfig.json",
        "diagnostics": false
      }
    }
  }
}
