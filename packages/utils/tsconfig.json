{
  "extends": "../../tsconfig.json",
  "include": ["src/**/*.ts", "test/**/*.ts"],
  "exclude": ["dist"],
  "compilerOptions": {
    "rootDir": ".",
    "types": ["node", "jest"],
    "paths": {
      "@sentry/*": [
        "../*/src"
      ]
    }
  }
}
