comment: false

coverage:
  status:
    project:
      default:
        enabled: false
    patch:
      default:
        informational: true
  ignore:
    - 'packages/deno/**'

config:
  files: changed
